import React, { useState } from 'react'
import CommonButton from '../../UI/button/CommonButton'

function AdditionBtnItem({...props}) {
  return (
    <>
      <CommonButton {...props}/>
    </>
  )
}

export default AdditionBtnItem
